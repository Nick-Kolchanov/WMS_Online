@model InventarizationReason
@{
    if (Model != null && Model.Id != 0) 
        ViewData["Title"] = "Изменение причины инвентаризации";
    else
        ViewData["Title"] = "Добавление причины инвентаризации";
}

<h1>@ViewData["Title"]:</h1>

<form method="post">
    <div asp-validation-summary="ModelOnly"></div>

    @Html.EditorForModel()

    @if(Model != null && Model.Id != 0)
        @Html.Hidden("id", Model.Id)
    
    <p>
        <input class="btn btn-default" type="submit" value="Отправить" />
    </p>
</form>
<form asp-action="InventarizationReasonTable" method="get">
    <input class="btn btn-default" type="submit" value="Назад" />
</form>
